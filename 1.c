#include <stdio.h>      /* printf */
#include <stdlib.h>     /* system, NULL, EXIT_FAILURE */
#include<iostream> 
#include<fstream>
#include<cstring> 
#include<map> 

using namespace std;
class OnMyKneeSir{
public : 

void add(){}
void runCommands(const string &s){
if(m[s]){
}

};

};

int main ()
{
  int i;
  fstream fileLoding ; 
  fileLoading.open("Map.txt");
  map<string,string> m ; 
  if (system(NULL)) puts ("Ok");
    else exit (EXIT_FAILURE);
  
  while(1){
  string inputCmd;
  cout<<"On My Knees Sir>";
  getline(cin,inputCmd);
  runCommands(inputCmd);
  
  runCommands()
  }
  return 0;
}
